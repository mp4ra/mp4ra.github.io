import { DataDisplay } from "@/components";

export const metadata = {
    title: "Checksum Types"
};

# Checksum types

This section documents the checksum types, used to identify the checksum used for data integrity in partial file format. These codes are documented here.

## Partial File Format checksum code points

<DataDisplay csv="checksum-types" />
