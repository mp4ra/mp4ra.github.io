import { DataDisplay } from "@/components";

export const metadata = {
    title: "Track References"
};

# Track references

This section documents the track-reference types, which link tracks with relationships between them. The link is typed with a code. These codes are documented here.

## ISO Family code points

> see [below](#quicktime-code-points) for QuickTime code points

<DataDisplay csv="track-references" />

## QuickTime code-points

<DataDisplay csv="track-references-qt" />
